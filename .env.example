PORT=4000
SESSION_SECRET=replace-with-32-random-bytes
DATA_FILE=work/data.json
UPLOAD_DIR=work/uploads
# Optional: set to a real bot token from @BotFather to enable a live Telegram
# channel (long-polling, no public URL needed). Leave blank to keep chat
# HTTP-only/simulated, as today.
TELEGRAM_BOT_TOKEN=
